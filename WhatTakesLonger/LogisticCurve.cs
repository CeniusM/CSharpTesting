using System;

namespace WhatTakesLonger
{
    class Test
    {
        // settings
        const int turns = 10;
        const int numbers = 10;
        public static void Start()
        {
            Random rnd = new Random();

            // create 3 difrent number arrays, one with ints, one with whole floats such as 3f, and float with decimal numbers
            // and then after run all the logistic curve code with the numbers and see how fast it is
        }
    }
}